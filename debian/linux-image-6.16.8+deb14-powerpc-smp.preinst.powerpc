#!/bin/sh -e

<<<<<<<< HEAD:debian/linux-image-6.12.38+kali-powerpc-smp.preinst
version=6.12.38+kali-powerpc-smp
========
version=6.16.8+deb14-powerpc-smp
>>>>>>>> debian:debian/linux-image-6.16.8+deb14-powerpc-smp.preinst.powerpc
image_path=/boot/vmlinux-$version

if [ "$1" = abort-upgrade ]; then
    exit 0
fi

if [ "$1" = install ]; then
    # Create a flag file for postinst
    mkdir -p /lib/modules/$version
    touch /lib/modules/$version/.fresh-install
fi

linux-run-hooks image preinst $version $image_path -- "$@"

exit 0
