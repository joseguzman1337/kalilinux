#!/bin/sh -e

<<<<<<<< HEAD:debian/linux-image-6.17.10+kali-mips32r2eb.postinst.mips
version=6.17.10+kali-mips32r2eb
========
version=6.18.3+deb14-armmp-lpae
>>>>>>>> debian:debian/linux-image-6.18.3+deb14-armmp-lpae.postinst.armhf
image_path=/boot/vmlinuz-$version

if [ "$1" != configure ]; then
    exit 0
fi

depmod $version

if [ -f /lib/modules/$version/.fresh-install ]; then
    change=install
else
    change=upgrade
fi
linux-update-symlinks $change $version $image_path
rm -f /lib/modules/$version/.fresh-install

linux-run-hooks image postinst $version $image_path -- "$@"

exit 0
