#!/bin/sh -e

<<<<<<<< HEAD:debian/linux-image-6.17.10+kali-loongson-3.preinst.mipsel
version=6.17.10+kali-loongson-3
========
version=6.18.3+deb14-cloud-amd64
>>>>>>>> debian:debian/linux-image-6.18.3+deb14-cloud-amd64-unsigned.preinst.amd64
image_path=/boot/vmlinuz-$version

if [ "$1" = abort-upgrade ]; then
    exit 0
fi

if [ "$1" = install ]; then
    # Create a flag file for postinst
    mkdir -p /lib/modules/$version
    touch /lib/modules/$version/.fresh-install
fi

linux-run-hooks image preinst $version $image_path -- "$@"

exit 0
