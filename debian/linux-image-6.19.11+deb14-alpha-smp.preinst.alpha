#!/bin/sh -e

<<<<<<<< HEAD:debian/linux-image-6.18.12+kali-cloud-amd64.preinst.amd64
version=6.18.12+kali-cloud-amd64
========
version=6.19.11+deb14-alpha-smp
>>>>>>>> debian:debian/linux-image-6.19.11+deb14-alpha-smp.preinst.alpha
image_path=/boot/vmlinuz-$version

if [ "$1" = abort-upgrade ]; then
    exit 0
fi

if [ "$1" = install ]; then
    # Create a flag file for postinst
    mkdir -p /lib/modules/$version
    touch /lib/modules/$version/.fresh-install
fi

linux-run-hooks image preinst $version $image_path -- "$@"

exit 0
